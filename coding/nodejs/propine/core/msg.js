const option = require("./option");

module.exports = {
    TOKEN_NOT_SUPPORTED: 'This token is not currently supported.',
    SUPPORTED_TYPE_ARE: 'Supported token types are: ' + option.supportTokenType,
    CALCULATE_PORTFOLIO_ALL: '(Question 1.1) Calculate all portfolio balance.',
    CALCULATE_PORTFOLIO_BY_TOKEN: '(Question 1.2) Calculate all portfolio balance by Token.',
    CALCULATE_PORTFOLIO_BY_DATE: '(Question 1.3) Calculate all portfolio balance in a Date. Date should be in YYYY-MM-DD format.',
};
